git add<文件名字>添加文件

git status 告诉你文件是否有被修改过

git diff 可以查看修改的内容

git commit -m""  简单解释一下`git commit`命令，`-m`后面输入的是本次提交的说明，可以输入任意内容，当然最好是有意义的，这样你就能从历史记录里方便地找到改动记录

git log  查看所有修改

git reset 将当前版本退回到之前的版本
